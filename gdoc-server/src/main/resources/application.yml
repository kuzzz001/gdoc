spring:
  application:
    name: gdoc-server
  profiles:
    active: dev
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB

gdoc:
  version: 1.0.0
  build-time: ${maven.build.timestamp:unknown}
