# Gdoc 需求文档

> 文档版本：v1.5.0 | 最后更新：2026-05-27

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户角色](#2-用户角色)
3. [功能模块](#3-功能模块)
4. [功能需求详解](#4-功能需求详解)
5. [非功能需求](#5-非功能需求)
6. [数据需求](#6-数据需求)
7. [约束与假设](#7-约束与假设)
8. [待办与规划](#8-待办与规划)

---

## 1. 产品概述

### 1.1 产品定位

Gdoc（吉智文档）是一款面向个人和小团队的**多人实时协同文档编辑系统**，提供类 Google Docs 的在线文档体验，集成即时通讯与社交功能。

### 1.2 核心价值

| 维度 | 描述 |
|------|------|
| 实时协作 | 多人同时编辑同一文档，毫秒级同步，OT 算法保证内容一致性 |
| 社交协作 | 好友系统 + 即时通讯 + 文档协作邀请，建立协作闭环 |
| 轻量部署 | 基于 Docker Compose 一键启动，MySQL/PostgreSQL 双数据库支持 |
| 版本管理 | 自动快照 + 历史回滚，防止数据丢失 |

### 1.3 目标用户

- **个人用户**：记笔记、写文档、个人知识管理
- **小团队**：3–20 人的团队协作，项目文档、会议纪要、技术方案
- **学生群体**：小组作业协作、论文写作

---

## 2. 用户角色

| 角色 | 描述 | 核心权限 |
|------|------|----------|
| 未登录用户 | 未认证的访客 | 浏览分享链接（只读），无其他操作权限 |
| 注册用户 | 已登录的普通用户 | 创建文档、管理个人文档、添加好友、收发消息 |
| 文档拥有者 | 文档的创建者 | 文档所有操作：编辑、删除、分享、管理协作者 |
| 文档编辑者 | 被邀请协作的用户 | 编辑文档内容，不可删除/分享文档 |
| 文档查看者 | 通过分享链接或受邀查看的用户 | 只读查看文档内容 |

---

## 3. 功能模块

```
gdoc/
├── 用户模块          — 注册、登录、信息管理
├── 文档模块          — 文档 CRUD、富文本编辑
├── 协同模块          — 实时编辑、OT 算法、光标同步
├── 分享模块          — 分享链接、权限控制
├── 社交模块          — 好友系统、即时通讯、协作邀请
├── 版本管理模块      — 自动快照、历史回滚
└── 主题模块          — 亮色/暗色/跟随系统主题切换
```

---

## 4. 功能需求详解

### 4.1 用户模块

#### FR-001 用户注册

| 项 | 描述 |
|------|------|
| 前置条件 | 未登录 |
| 输入 | 密码、昵称（可选） |
| 流程 | 系统自动分配 6 位账号（000001–999999）→ 校验账号唯一性 → 写入数据库 |
| 后置条件 | 用户创建成功，返回分配的账号信息 |
| 异常 | 账号分配冲突（并发注册），自动重试最多 100 次 |
| 规则 | 密码 BCrypt 加密存储；昵称为空时默认"用户+账号" |

#### FR-002 用户登录

| 项 | 描述 |
|------|------|
| 前置条件 | 未登录 |
| 输入 | 6 位账号 + 密码 |
| 流程 | 账号查询 → 密码校验 → 生成 JWT Token（含 userId、username） |
| 后置条件 | 前端保存 Token 至 localStorage，跳转首页 |
| 异常 | 账号不存在、密码错误、账号已被禁用 |

#### FR-003 头像管理

| 项 | 描述 |
|------|------|
| 功能 | 选择默认头像 / 上传自定义头像 |
| 默认头像 | 8 种 SVG 头像（4 男 4 女，各具风格配色） |
| 上传规则 | 仅 JPG/PNG，最大 5MB，推荐 1:1 比例 200×200px+ |
| 后处理 | 后端自动裁剪压缩，存储至 uploads 目录 |

---

### 4.2 文档模块

#### FR-004 文档创建

| 项 | 描述 |
|------|------|
| 输入 | 标题（可选），默认为"无标题文档" |
| 流程 | 创建文档 → 写入数据库 → 返回 DocumentVO |
| 后置 | 跳转至编辑器页面 |

#### FR-005 文档编辑

| 项 | 描述 |
|------|------|
| 编辑器 | 基于 contenteditable 的富文本编辑器 |
| 格式支持 | 字体、字号、粗体、斜体、下划线、删除线、颜色、背景色 |
|  | 对齐方式（左/中/右）、有序/无序列表、标题（H1–H3） |
|  | 引用、链接、水平分割线、撤销/重做 |
| 保存 | 自动保存（内容变化 3 秒后触发）+ 手动保存按钮 |
| 快捷键 | Ctrl+S 保存，Ctrl+B/U/I 等标准编辑快捷键 |

#### FR-006 文档列表

| 项 | 描述 |
|------|------|
| 展示 | 卡片网格布局，显示标题、更新时间、权限标签 |
| 排序 | 按更新时间倒序 |
| 范围 | 我创建的 + 我协作的文档 |
| 分页 | 支持分页查询（默认每页 12 条） |
| 操作 | 点击编辑、确认删除 |

#### FR-007 文档删除

| 项 | 描述 |
|------|------|
| 权限 | 仅文档拥有者可删除 |
| 方式 | 物理删除（从数据库移除） |
| 操作 | 二次确认弹窗 |

---

### 4.3 协同模块

#### FR-008 实时协同编辑

| 项 | 描述 |
|------|------|
| 协议 | WebSocket + STOMP（SockJS fallback） |
| 房间模型 | 每个文档对应一个协同房间（Room） |
| 流程 | 用户加入房间 → 同步当前内容 → 实时推送操作 → 离开房间 |
| OT 算法 | 自研纯文本 OT，支持 INSERT / DELETE / RETAIN 三种操作类型 |
| 操作合并 | 连续 INSERT 自动合并为单次操作 |

#### FR-009 光标同步

| 项 | 描述 |
|------|------|
| 功能 | 实时同步协作者光标位置 |
| 信息 | 用户 ID、用户名、光标颜色、位置坐标 |
| 更新 | 每次 cursor 移动事件触发 |

#### FR-010 操作冲突解决

| 项 | 描述 |
|------|------|
| 机制 | OT Transform 函数：transform(server_op, client_op) → (server_op', client_op') |
| 版本管理 | 每次操作递增版本号，操作附带版本信息 |
| 功能 | compose（组合连续操作）、invert（操作反转，用于撤销） |

---

### 4.4 分享模块

#### FR-011 创建分享链接

| 项 | 描述 |
|------|------|
| 权限 | 仅文档拥有者可创建 |
| 参数 | 权限（view / editor）+ 有效期（可选小时数） |
| Token | UUID 去横线生成 32 位字符串 |
| 限制 | 分享链接不过期时永不过期 |

#### FR-012 查看分享文档

| 项 | 描述 |
|------|------|
| 入口 | 访问 `/share/{token}` |
| 鉴权 | 校验 Token 是否存在 → 是否过期 → 返回文档内容 |
| 视图 | 只读模式查看，不可编辑 |

#### FR-013 管理分享链接

| 项 | 描述 |
|------|------|
| 列表 | 查看某文档的所有分享链接 |
| 撤销 | 删除指定分享链接，再次访问返回 404 |

---

### 4.5 社交模块

#### FR-014 好友系统

| 项 | 描述 |
|------|------|
| 搜索 | 按昵称/账号模糊搜索用户（排除自身，上限 20 条） |
| 发送请求 | 双向去重校验，防止重复/自我添加 |
| 处理请求 | 接受/拒绝，仅被请求者可操作，幂等处理 |
| 删除好友 | 双向关系同步清理 |
| 列表 | 显示好友昵称、头像、未读消息数 |

#### FR-015 即时通讯

| 项 | 描述 |
|------|------|
| 消息类型 | 文本、图片（JPG/PNG ≤10MB）、文件（≤50MB）、系统消息 |
| 实时推送 | WebSocket（`/ws/chat`），断线 3 秒自动重连 |
| 消息状态 | 已发送（0）→ 已送达（1）→ 已读（2） |
| 未读计数 | 按好友统计 + 总计，15 秒轮询刷新 |
| 历史消息 | 按时间查询聊天记录 |
| UI 风格 | 微信风格消息气泡（左/右布局、绿色/白色配色） |

#### FR-016 协作邀请

| 项 | 描述 |
|------|------|
| 范围 | 仅可邀请好友 |
| 参数 | 选择文档 + 权限（editor/viewer）+ 附言（可选） |
| 防重复 | 待处理邀请不可重复发送 |
| 操作 | 邀请者可取消，受邀者可接受/拒绝 |
| 自动授权 | 接受邀请后自动添加为文档协作者 |

---

### 4.6 文件夹系统（v1.3.0 新增）

#### FR-019 文件夹管理

| 项 | 描述 |
|------|------|
| 结构 | 多级文件夹树形结构（parent_id 自引用） |
| 操作 | 创建文件夹、重命名、删除（子文件夹移至根目录） |
| 排序 | 支持自定义排序字段 |
| 权限 | 每个用户独立文件夹空间，按 owner_id 隔离 |
| 数据库 | `gdoc_folder` 表（MySQL/PostgreSQL） |

---

### 4.7 评论系统（v1.3.0 新增）

#### FR-020 文档评论

| 项 | 描述 |
|------|------|
| 范围评论 | 支持选中文本范围（range_start/range_end）添加评论 |
| 评论回复 | 支持评论下多轮回复对话 |
| 状态管理 | 已解决/未解决状态切换 |
| 权限 | 文档的创建者和协作者均可评论 |
| 数据库 | `gdoc_comment` + `gdoc_comment_reply` 表 |

---

### 4.8 通知系统（v1.3.0 新增）

#### FR-021 系统通知

| 项 | 描述 |
|------|------|
| 通知类型 | collab_invite（协作邀请）/ comment（评论）/ doc_shared（分享）/ mention（@提及） |
| 状态管理 | 已读/未读标记、全部已读、未读计数 |
| 关联数据 | 支持关联文档/评论 ID（related_id） |
| 持久化 | `gdoc_notification` 表，按用户+时间排序 |
| 前端 | notificationApi 接口层 |

---

### 4.9 版本管理模块

#### FR-017 快照创建

| 项 | 描述 |
|------|------|
| 触发 | 每 50 次操作变更自动创建快照 |
| 内容 | 记录当前版本完整内容 JSON |
| 关联 | 快照关联操作序列号，用于精准定位 |

#### FR-018 历史回滚

| 项 | 描述 |
|------|------|
| 列表 | 查询文档的历史版本快照列表 |
| 回滚 | 指定版本号，将文档内容恢复至该快照 |
| 规则 | 回滚后版本号递增（产生新版本） |

---

### 4.7 主题模块

#### FR-019 主题切换

| 项 | 描述 |
|------|------|
| 模式 | 亮色（Light）/ 暗色（Dark）/ 跟随系统（System） |
| 切换方式 | 点击 ThemeToggle 按钮三态循环切换 |
| 持久化 | 用户选择写入 localStorage，下次访问恢复 |
| 系统跟随 | 监听 `prefers-color-scheme` media query 变化 |

#### FR-020 CSS 变量系统

| 项 | 描述 |
|------|------|
| 设计变量 | 颜色（primary/success/warning/danger） |
| | 文本（primary/secondary/placeholder/disabled） |
| | 背景（primary/secondary/tertiary） |
| | 边框、圆角、阴影、过渡动画 |
| 适配方式 | 亮色/暗色自动切换，全局统一 |

---

## 5. 非功能需求

### 5.1 性能指标

| 指标 | 要求 |
|------|------|
| API 响应时间 | 99% 的 API 请求 ≤ 500ms |
| WebSocket 延迟 | OT 操作端到端延迟 ≤ 200ms（同区域） |
| 并发编辑 | 支持 20+ 人同时编辑同一文档 |
| 页面加载 | 首屏加载 ≤ 3s（GZip 后 ~60KB JS） |
| 文档大小 | 单文档建议 ≤ 5MB |

### 5.2 安全要求

| 类别 | 要求 |
|------|------|
| 认证 | JWT Token 认证，Token 有效期 24h |
| 密码 | BCrypt 加密存储，不记录明文 |
| 权限 | 文档级权限控制（owner/editor/viewer） |
| 文件上传 | 限制类型（JPG/PNG），限制大小（5MB/50MB） |
| 跨域 | Spring Security CSRF 防护 |

### 5.3 可用性要求

| 指标 | 要求 |
|------|------|
| 服务可用性 | 99.9%（计划内维护除外） |
| 数据持久性 | 操作日志 + 自动快照，任何操作不丢失 |
| 容灾 | Redis 宕机后协同功能降级，文档编辑不受影响 |
| 数据库 | MySQL/PostgreSQL 双数据库支持 |

### 5.4 可维护性

| 类别 | 要求 |
|------|------|
| 架构 | 模块化（9 个 Maven 模块），模块间松耦合 |
| 日志 | 关键操作有日志记录，包含操作人、时间、参数 |
| API 文档 | Knife4j 自动生成接口文档 |
| 部署 | Docker Compose 一键启动，Render Blueprint 云部署 |

### 5.5 兼容性

| 类别 | 要求 |
|------|------|
| 浏览器 | Chrome 90+、Firefox 90+、Safari 14+、Edge 90+ |
| 后端 | JDK 17+，Spring Boot 3.2.5 |
| 数据库 | MySQL 8.0+ / PostgreSQL 16+ |
| 前端 | Vue 3 + Vite 8 + TypeScript 6 |

---

## 6. 数据需求

### 6.1 实体关系概览

```
User (1) ──── owns ────> Document (N)
User (1) ──── collaborates ────> Document (N)  [通过 Collaborator 关联表]
User (1) ──── friend ────> User (N)  [通过 Friendship 关联表]
User (1) ──── sends ────> Message (N)
User (1) ──── invites ────> User (N)  [通过 CollabInvitation 关联表]
Document (1) ──── has ────> Snapshot (N)
Document (1) ──── has ────> OperationLog (N)
Document (1) ──── has ────> Share (N)
```

### 6.2 核心实体

| 实体 | 表名 | 说明 |
|------|------|------|
| User | `gdoc_user` | 用户账号、密码、昵称、头像 |
| Document | `gdoc_document` | 文档标题、内容、版本号、逻辑删除 |
| Share | `gdoc_share` | 分享 Token、权限、过期时间 |
| Collaborator | `gdoc_collaborator` | 用户-文档关联、角色（editor/viewer） |
| Snapshot | `gdoc_snapshot` | 版本快照内容 |
| OperationLog | `gdoc_operation_log` | 操作 JSON 记录 |
| Friendship | `gdoc_friendship` | 好友关系及状态流转 |
| Message | `gdoc_message` | 消息内容、类型、状态 |
| CollabInvitation | `gdoc_collab_invitation` | 协作邀请信息 |

### 6.3 数据量预估

| 表 | 单用户年增长 | 备注 |
|------|------|------|
| Document | ~50 条 | 按活跃用户计算 |
| Snapshot | ~500 条 | 每 50 次操作产生一个快照 |
| Message | ~1000 条 | 按每天 3 条消息计算 |
| OperationLog | ~10000 条 | OT 操作日志，定期归档 |
| Friendship | ~20 条 | 好友数量上限 |

---

## 7. 约束与假设

### 7.1 技术约束

- 前端富文本编辑器使用 contenteditable + document.execCommand（已废弃 API，计划迁移至 TipTap）
- OT 算法当前仅支持纯文本，不支持富文本结构化操作
- 消息推送依赖 Redis Pub/Sub，多节点部署时需 Redis 支持
- 图片/文件上传存储在本地文件系统，不适合多节点水平扩展（需接入对象存储）

### 7.2 业务约束

- 账号系统采用 6 位自增数字，上限 999,999 用户
- 好友上限未做硬性限制（建议不超过 500）
- 单文档协作人数建议不超过 50 人
- 未实现操作日志的自动归档和清理

### 7.3 假设

- 用户网络环境良好（WebSocket 连接稳定）
- 用户平均文档大小在 1MB 以内
- 同一文档的并发编辑用户不超过 20 人
- 部署环境可访问 Redis 和 MySQL/PostgreSQL

---

## 8. 待办与规划

详见 [TODO.md](TODO.md) 完整开发计划。

### 近期规划（v1.3.x）

| 优先级 | 功能 | 预估工作量 |
|--------|------|-----------|
| P0 | TipTap 编辑器引擎迁移 | 5 人日 |
| P0 | 富文本 OT 算法升级 | 5 人日 |
| P1 | 文档导入导出（PDF/Word/MD） | 3 人日 |
| P1 | 文件夹系统 + 搜索 | 3 人日 |

### 长期规划（v2.x）

| 功能 | 说明 |
|------|------|
| 团队/组织协作 | 团队文档库、权限继承 |
| 行内评论系统 | 选中文本添加评论、@提及 |
| AI 智能（续写/润色/翻译） | 大模型集成 |
| 企业级功能 | SSO、水印、审计日志 |